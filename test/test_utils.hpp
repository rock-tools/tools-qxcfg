#ifndef TEST_UTILS
#define TEST_UTILS

#include <string>
/**
 * Retrieve the root directory by analysing the current execution path:
 * returns /../<project-name>/ as path
 */
std::string getRootDir();
#endif
